# Data

The static structure of the system — what entities exist and how they are stored.

## Contents

- **models/** — ORM and Pydantic schema contracts for every database entity.
- **knowledge/** — The Handbook system: how Markdown files are stored, linked, versioned, and health-scored.

Read `models/` first if working on the database layer. Read `knowledge/` if working on the Handbook or any knowledge-loading feature.
