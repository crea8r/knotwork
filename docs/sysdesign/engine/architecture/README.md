# Architecture

The big picture before the detail.

- **overview.md** — System components, their responsibilities, and how they connect.
- **data-flow.md** — End-to-end trace of a run: API call → worker → LangGraph → node execution → WebSocket → UI.
